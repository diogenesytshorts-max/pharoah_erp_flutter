import 'package:flutter/material.dart';
class ComplianceHub extends StatelessWidget {
  const ComplianceHub({super.key});
  @override build(context) => Scaffold(appBar: AppBar(title: const Text("Compliance Hub"), backgroundColor: Colors.teal.shade800));
}

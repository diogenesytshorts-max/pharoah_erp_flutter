name: pharoah_erp
description: Pharoah ERP Professional Billing & GST Inventory Software.
version: 1.0.5+1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Core Logic & Storage
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  path_provider: ^2.1.2
  provider: ^6.1.1

  # PDF & Printing
  pdf: ^3.10.7
  printing: ^5.11.0
  
  # Data Exchange & Sharing
  share_plus: ^7.2.2
  csv: ^6.0.0
  file_picker: ^8.1.2
  file_saver: ^0.2.11  # Naya package Downloads mein save karne ke liye

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  path_provider: ^2.1.2
  provider: ^6.1.1
  pdf: ^3.10.7
  printing: ^5.11.0
  share_plus: ^7.2.2
  csv: ^6.0.0
  file_saver: ^0.2.11
  # AI & Camera Packages
  image_picker: ^1.0.7
  file_picker: ^8.1.2
  google_mlkit_text_recognition: ^0.11.0
  http: ^1.2.0

# App Icon Configuration
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/logo.png"

flutter:
  uses-material-design: true

  # Assets configuration
  assets:
    - assets/logo.png

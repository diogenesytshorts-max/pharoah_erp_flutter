name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      
      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'microsoft' # Microsoft JDK is extremely stable on GitHub
          java-version: '17'
          
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          flutter-version: '3.22.0' # Aap ek specific version de sakte hain ya 'any'
          
      - name: Get Dependencies
        run: |
          flutter clean
          flutter pub get
        
      - name: Build Release APK
        run: flutter build apk --release --no-tree-shake-icons
        
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4 # Naam sahi hona chahiye
        with:
          name: release-apk
          path: build/app/outputs/flutter-apk/app-release.apk
